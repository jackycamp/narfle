//
// ContentView.swift
// 
// System entry point into the app.
// This View should be kept minimal.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        AppView()
    }
}

#Preview {
    ContentView()
}
