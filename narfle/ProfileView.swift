import SwiftUI

struct ProfileView: View {
    @State private var showDevInfo = false

    var body: some View {
        VStack {
            Text("Profile")
            .font(.title3)
        }
    }
}

